<?php
echo $_SERVER['REQUEST_URI'];
// Unset all of the session variables
$_SESSION = array();

// Destroy the session.
session_destroy();
setcookie('login', '', 1);
// setcookie('vk', '', 1);


header("Location: /"); /* Redirect browser */
exit();
